PanelHeader — the chrome header on every side panel. Eyebrow label left, mono count right.

```jsx
<PanelHeader label="Seeds" count="6 results" />
<PanelHeader label="Configure step" count="FWD-02" />
```
