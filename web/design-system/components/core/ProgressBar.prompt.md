ProgressBar — green fill on a cream track. Pass `value` (0–100) for a known amount, omit it for the live indeterminate bar used while a step is "working".

```jsx
<ProgressBar value={62} />
<ProgressBar /> {/* indeterminate */}
```
