Badge — a mono pill for status ("running"), counts ("5 filters"), and short states. Use `accent` (green) only for live/active meaning.

```jsx
<Badge tone="accent">running</Badge>
<Badge>6 papers</Badge>
<Badge tone="danger">Rejected</Badge>
```
